# Google Map Tiles API key with the "Map Tiles API" enabled, restricted to localhost.
# Copy this file to .env.local and fill it in. Without it the app shows the
# keyless terrain preview instead of the photorealistic mesh.
VITE_GOOGLE_MAPS_TILES_KEY=
